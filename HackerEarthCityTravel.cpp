#include <bits/stdc++.h>
#define lli long long int

using namespace std;

int main()
{
    lli distance, daily, exceptions, EDay, EDist, result = 0;
    cin >> distance >> daily >> exceptions;

    map <lli, lli> EMap;
    while(exceptions --)
    {
        cin >> EDay >> EDist;
        EMap.insert(pair <lli, lli>)(EDay, EDist));
    }

    map <lli, lli> :: iterator itr;


    for(itr = EMap.begin(), itr != Emap.end(); ++itr;)
    {
        long long long int
    }

    return 0;
}
